enum CoreEvents {
    Destroy = "Destroy",
}

enum CansoleEvents {
    Hide = "Hide",
    Show = "Show",
}

enum MouseEvents {
    Active = "Active",
    ActiveLost = "ActiveLost",
    Click = "Click",
    Drag = "Drag",
    Hover = "Hover",
    HoverLost = "HoverLost",
}

export { CansoleEvents, CoreEvents, MouseEvents };
